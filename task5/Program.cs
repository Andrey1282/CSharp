int N = 100;
int i = 1;
while (i <= N)
{
    //Console.WriteLine(i); //выводит в столбец
    Console.Write(i); // выводит в строку
    Console.Write(' '); //выводит символ типа char (в данном случае "пробел")
    i = i + 1; // увеличивает значение переменной i на 1
}
